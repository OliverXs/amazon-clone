export const initialState = {
    kart:[],
}

export function reducer(state,action){
    switch(action.type){
        case 'ADD_TO_KART':
            break;
        case 'REMOVE_FROM_KART':
            break;
        default:
            return state;
    }
}